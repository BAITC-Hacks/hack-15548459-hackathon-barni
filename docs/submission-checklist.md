# Чеклист готовности к сдаче

## Статус требований кейса

**Полное покрытие требований пока подтвердить нельзя:** `case/case.md` по-прежнему содержит только «Кейс ещё не выдан». В нём нет проверяемого описания пользователя, задачи, предметных ограничений и обязательных результатов. Поэтому в этом документе перечислены подтверждённые артефакты AML-проекта, а не выдуманные must-have организаторов.

## Проверенные артефакты и команды

Последний запуск финальной автоматической проверки на чистом клоне `origin/main` на момент проверки завершился успешно: подготовлены venv и зависимости, пайплайн занял **13,456 с** по монотонным часам (лимит скрипта — 300 с), итоговая таблица скрипта — **9/9 ✅**, код завершения — **0**.

| Артефакт / проверка | Файл или команда | Результат |
|---|---|---|
| Входные данные | `data/nodes.parquet`, `data/edges.parquet`, `data/transactions.parquet` | Присутствуют в проверенной копии. Соответствие кейсу неизвестно, пока не выдано ТЗ. |
| Повторный расчёт результатов | `python run_pipeline.py` (также запускается `final_check.sh`) | Успешно в последнем прогоне чистого клона; 13,456 с при лимите 300 с. |
| Роли узлов, кластеры, топ, метрики, карточки и отчёт | `outputs/nodes_roles.csv`, `outputs/clusters.csv`, `outputs/top_nodes.csv`, `outputs/metrics.parquet`, `outputs/node_cards.md`, `outputs/report.md` | Созданы пайплайном в чистом клоне; проверка `check_outputs` прошла. |
| CSV-схемы и содержимое | `python tests/check_outputs.py` | Passed в `final_check.sh`. |
| Тест пайплайна | `tests/test_pipeline.py` | Passed: 2 теста в `final_check.sh`. |
| Smoke-проверки интерфейсов | `tests/test_ui_smoke.py` | Passed для `ui/app.py` и `ui/viewer.py` в `final_check.sh`. |
| Smoke-тест харнеса | `python tests/smoke_test.py` | Отдельный запуск прошёл в предыдущем прогоне; он не является частью указанного результата `final_check.sh`. |
| Секреты в проверенной копии | проверка `final_check.sh` | `.env` отсутствовал; tracked-файлов с `sk-` префиксом не обнаружено. |
| Методика, роли и ограничения интерпретации | `docs/roles.md` | Документ присутствует; результаты описаны как признаки/гипотезы для проверки, а ограничения данных задокументированы. |
| Инструкция запуска и скрипт | `docs/quickstart.md`, `run.sh` | Присутствуют. `run.sh` успешно запускался в предыдущей проверке демо на чистом клоне, включая ответ HTTP 200. В текущий `final_check.sh` запуск `run.sh` не входил; интерактивный запуск на `origin/main` на момент проверки отдельно не повторялся. |
| Основное приложение и просмотрщик | `ui/app.py`, `ui/viewer.py` | Оба прошли `test_ui_smoke`. Ручная проверка демо-сценария не подтверждена. |

## Предварительная инвентаризация AML-поставки

Эти пункты выведены из текущей реализации и документации проекта. Их нужно сверить с ТЗ, когда оно появится; они **не подтверждены как требования организаторов**.

| Предполагаемый результат | Где показать | Текущий статус |
|---|---|---|
| Анализ транзакционного графа | `data/*.parquet` → `python run_pipeline.py` | Чистый прогон прошёл; связь данных с будущим кейсом не установлена. |
| Объяснимые роли и приоритеты узлов | `outputs/nodes_roles.csv`, `outputs/top_nodes.csv`, `outputs/node_cards.md` | Файлы сформированы; механические проверки прошли. |
| Кластеры и сводные результаты | `outputs/clusters.csv`, `outputs/metrics.parquet`, `outputs/report.md` | Файлы сформированы пайплайном; CSV-проверка прошла. |
| Интерактивный просмотр | `streamlit run ui/app.py` или `streamlit run ui/viewer.py` | Автотесты интерфейсов прошли; ручной полный сценарий не подтверждён. |
| AI-ассистент | Вкладка в `ui/app.py`; настройка ключа описана в `docs/quickstart.md` | Реализация есть. В проверенном клоне `.env` не было; работа с реальным API-ключом этим прогоном не подтверждена. |
| Повторяемый запуск | `docs/quickstart.md`, `run.sh` | Инструкция есть; пайплайн прошёл в текущей clean-clone проверке, а `run.sh` успешно запускался в предыдущей проверке демо с HTTP 200. Интерактивный запуск на `origin/main` на момент текущей проверки не повторялся. |

## Что остаётся сделать

- Получить ТЗ и внести его в `case/case.md`; до этого полный список обязательных пунктов и соответствие им подтвердить нельзя.
- После получения ТЗ сопоставить каждый must-have с конкретным файлом, экраном или командой и повторно обновить эту таблицу.
- Провести ручной прогон демонстрации по реальным требованиям кейса. Автотесты интерфейсов не подтверждают готовность самого сценария выступления.
- `docs/demo.md` и `docs/pitch.md` сейчас отсутствуют. Их подготовка — условный следующий шаг, если этого потребует ТЗ или план выступления; наличие этих файлов не установлено как требование организаторов.
- Если `final_check.sh` должен входить в сдаваемую версию, перенести/слить его в `main`: результат относится к `origin/main` на момент проверки, а сам скрипт будет закоммичен в ветке `demo` и ещё не находится в `main`.

## После получения ТЗ

1. Записать в `case/case.md` пользователя, проблему, доступные данные, действия агента, ожидаемые результаты и ограничения.
2. Добавить по каждому обязательному требованию точное соответствие артефакту или команде, а также результат проверки.
3. Проверить, что команда запуска и необходимые артефакты соответствуют требованиям кейса; сохранить результаты фактических проверок.
4. Подготовить и отрепетировать демо/питч, если они нужны по ТЗ или формату сдачи.
