"""ОБРАЗЕЦ инструмента. Скопируйте: cp _template.py my_tool.py — и он сразу у агента.

- Имя функции = имя инструмента (snake_case, английский).
- Docstring = инструкция для модели: ЧТО делает и КОГДА вызывать. Это важнее кода.
- Подсказки типов обязательны (str, int, float, bool, list). Описание параметров — в Args.
- Вернуть str / dict / list. Ошибки просто raise — агент увидит текст и попробует исправиться.
- Для структурированного анализа внутри инструмента: from ..structured import extract
- Для вызова другой модели:                          from ..router import ROUTER
"""
from . import tool


@tool
def example_tool(city: str, days: int = 1) -> dict:
    """Пример: прогноз погоды для города. Вызывай, когда пользователь спрашивает о погоде.

    Args:
        city: Название города, например "Астана".
        days: На сколько дней прогноз.
    """
    return {"city": city, "days": days, "forecast": "солнечно (заглушка)"}
